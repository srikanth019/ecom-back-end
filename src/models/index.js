const { User } = require("./user")
const { Product } = require("./product")
const { Category } = require("./category")
const { Brand } = require("./brand")

module.exports = { Product, User, Category, Brand }